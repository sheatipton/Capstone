//
//  ImageGallery.swift
//  Capstone
//
//  Created by Shea Tipton on 2/1/23.
//

import SwiftUI

struct ImageGallery: View {
    /*@EnvironmentObject var captureview: CaptureView
    var body: some View {
        VStack {
            if let image = captureview.image {
                Image(uiImage: image)
                    .resizable()
                    .scaledToFit()
                    .frame(minWidth: 0, maxWidth: .infinity)
            } else {
                Image(systemName: "photo.fill")
                    .resizable()
                    .scaledToFit()
                    .opacity(0.6)
                    .frame(minWidth: 0, maxWidth: .infinity)
                    .padding(.horizontal)
            }
        }*/
    }
}

struct ImageGallery_Previews: PreviewProvider {
    static var previews: some View {
        ImageGallery()
    }
}
